<section id="{{ id }}" class="{{ class }}" style='background-image:url("{{ image }}")'>
	<div class='mask'></div>
	<div class="{{ row }}">
		<div class="{{ column_text }}">
			<h2>{{ title }}</h2>
			{{ content|raw }}
		</div>
	</div>
</section>
